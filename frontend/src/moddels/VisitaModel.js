export class Visita {
  constructor(id, data, hora, cidadeId, expositorId, pessoaId, vendedorId) {
    this.id = id;
    this.data = data;
    this.hora = hora;
    this.cidadeId = cidadeId;
    this.expositorId = expositorId;
    this.pessoaId = pessoaId;
    this.vendedorId = vendedorId;
  }
}